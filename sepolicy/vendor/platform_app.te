allow platform_app sysfs_fpsinfo:file rw_file_perms;
allow platform_app sysfs_thermal:dir r_dir_perms;
allow platform_app sysfs_thermal:file r_file_perms;

# Allow platform_app to get debug_mtk_gpud_prop
get_prop(platform_app, debug_mtk_gpud_prop)

# Address misc denials
allow platform_app zygote:unix_stream_socket { getopt };
